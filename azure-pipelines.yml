trigger:
  - master

resources:
  repositories:
    - repository: templates
      type: github
      name: RaaLabs/EdgeTools
      endpoint: RaaLabs

jobs:
  - template: AzureDevOps/DotNet/pipeline.yml
